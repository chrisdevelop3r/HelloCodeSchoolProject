{
  "name": "assessio-quote-builder",
  "private": true,
  "version": "0.0.0",
  "description": "Assessio quote builder — monorepo root",
  "license": "UNLICENSED",
  "packageManager": "pnpm@10.18.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "capture": "node tools/capture-fixtures.mjs",
    "capture:check": "node tools/capture-fixtures.mjs --check",
    "mutants": "node tools/mutation-check.mjs",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "verify": "pnpm typecheck && pnpm test"
  },
  "devDependencies": {
    "playwright": "1.63.0",
    "typescript": "5.9.3"
  }
}
